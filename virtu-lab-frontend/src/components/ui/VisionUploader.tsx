import React, { useRef } from 'react';

interface Props {
  onClose: () => void;
}

export const VisionUploader: React.FC<Props> = ({ onClose }) => {
  return (
    <div>
      {/* VisionUploader stub */}
    </div>
  );
};
